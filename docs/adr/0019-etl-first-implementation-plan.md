# ETL-First Implementation Plan

The first implementation phase will build and validate the covalent ETL before modifying PMDM. It must produce CovalentComplexRecord outputs, a Rule Calibration Sheet, an initial Reaction Family Rule Table, radius-bounded covalent edge candidates, leakage-aware splits, and visual inspection artifacts for target atoms and ligand attachment atoms. Model changes are deferred until these supervision artifacts exist.
