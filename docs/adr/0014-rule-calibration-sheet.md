# Rule Calibration Sheet

The first ETL will produce a Rule Calibration Sheet as a required artifact. For each reaction family, it will summarize sample counts, representative structures, target atom distributions, ligand attachment distributions, warhead distributions, SMARTS coverage, geometry summaries, outliers, manual decisions, and notes. This makes the Reaction Family Rule Table reproducible rather than hiding calibration choices inside scripts.
