# Rule-First Reaction Family Consistency

The first model will enforce reaction-family consistency with explicit rule masks for allowed attachment atoms, warhead local environments, covalent bond types, and geometry ranges. A learned classifier may be added later, but the first version uses auditable chemistry rules because the reaction family vocabulary is small and classifier-first consistency would risk learning dataset bias instead of chemical validity.
